---
description: 错误处理与空状态规范，约束失败反馈、重试和异常兜底
alwaysApply: true
---

# 错误处理与空状态规范

## 失败反馈

- 所有关键流程都应提供明确的失败反馈
- 不要只在控制台输出错误，而不向用户展示任何信息
- 对设备、网络、接口、实时连接等失败场景，要区分不同原因和不同处理方式

## 空状态

- 没有数据时必须展示空状态，不要留白或让用户猜测是否是页面未加载
- 空状态文案要说明当前没有内容的原因或下一步动作
- 如有必要，应提供刷新、重试或重新连接入口

## 异常兜底

- 对缺失字段、非法值和未知状态要有兜底处理
- 不要假设后端返回一定完整正确
- 关键展示区域不能因为单个字段异常而整体报错

## toolkit 场景

- 设备离线、网络中断、调试服务异常都属于高频异常场景，必须有可见处理方式
- 现场使用的终端界面应优先保证“出错后可恢复”，而不是仅提示失败