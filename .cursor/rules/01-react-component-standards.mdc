---
description: React 组件编写规范，约束页面拆分、职责边界与 JSX 写法
alwaysApply: true
---

# React 组件编写规范

## 组件职责

- 页面组件只负责组织布局、协调状态和组合子组件
- 复杂页面必须拆分为更小的子组件、hooks、services 和 types
- 单个组件不要同时承担数据请求、业务判断、状态管理和大量模板渲染

## JSX 规则

- 避免在 JSX 中堆叠过多条件判断和复杂表达式
- 复杂条件应提前抽成变量、函数或子组件
- 列表渲染时要提供稳定的 `key`
- 对于高频刷新的区域，优先使用更小粒度组件，减少无效重渲染

## Props 与类型

- 组件 props 必须显式定义类型，不允许随意使用 `any`
- 复杂对象建议使用接口或类型别名描述，避免在组件内部直接依赖接口返回结构
- 子组件对外只暴露必要参数，不要把内部状态实现细节透传出去

## Hooks 规则

- 自定义逻辑优先抽成 `hooks`
- 只有在确实需要跨组件共享或复用时才抽 hook，避免过度抽象
- `useEffect` 必须有清晰的副作用目的，避免用它同步派生状态
- 派生值优先使用 `useMemo` 或普通函数计算，不要重复存储

## 适配车载调试终端的要求

- 终端类页面通常信息密度高，组件应优先保障可读性和层次感
- 列表、卡片、状态面板、详情区等应保持清晰分区
- 面向设备状态、网络状态、告警状态的组件要预留错误态、空态、加载态
