---
description: UI 与样式规范，约束 AntD/MUI/Tailwind/Less 的使用边界和视觉一致性
alwaysApply: true
---

# UI 与样式规范

## 视觉一致性

- 以项目现有设计语言为准，保持整体风格统一
- 不要为了单个页面临时引入一套全新的视觉体系
- 组件间的间距、圆角、层级、阴影和动效应尽量保持一致

## 组件库使用边界

- `Ant Design`、`MUI`、`Tailwind CSS`、`Less` 可以共存，但必须遵循现有项目约定
- 不要无节制混用不同组件库的同类组件，避免视觉和交互不一致
- 能复用现有公共组件时，优先复用，而不是临时手写一个只用一次的新实现

## 样式书写

- 布局类样式可优先使用现有的工具类方案
- 复杂视觉效果应集中管理，避免大量行内样式散落在 JSX 中
- 业务状态颜色、告警颜色、连接颜色等应保持统一，不要每个页面自定义一套

## 车载终端 UI 要求

- 信息展示优先清晰、稳定、易读
- 空状态、加载态、异常态、离线态必须可见且语义明确
- 大屏或终端场景下，字号、对比度和卡片层次应优先保证可读性
- 动效应服务于状态反馈，不要喧宾夺主
