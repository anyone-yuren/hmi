---
description: 实时通信规范，约束 WebSocket、SignalR、订阅和连接生命周期
alwaysApply: true
---

# 实时通信规范

## 连接生命周期

- WebSocket、SignalR 或其他长连接必须统一管理生命周期
- 连接建立、重连、断开、销毁要有明确边界
- 不要在多个组件中重复创建同一连接实例
- 组件卸载时必须清理订阅、定时器和监听器

## 消息处理

- 原始消息不要直接驱动 UI，先做结构化处理和状态归一化
- 消息体要定义类型，避免在业务代码中大量使用裸对象和魔法字段
- 高频消息更新时，要注意节流、去重和最小刷新范围

## 可靠性要求

- 要考虑断线重连、连接失败、服务器无响应等情况
- 必须给用户明确的连接状态反馈，而不是静默失败
- 对于关键设备状态，刷新策略要尽量避免闪烁和状态抖动

## toolkit 场景注意

- 调试终端中的实时数据往往会影响操作决策，因此数据时效性和稳定性都很重要
- 任何实时面板都不应因为单条消息异常而导致整页崩溃
- 实时状态变化应与选中态、筛选态、分页态保持解耦
