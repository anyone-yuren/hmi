---
description: 项目总则，约束 toolkit 的工程边界、技术选型与输出风格
alwaysApply: true
---

# `apps/toolkit` 项目总则

`apps/toolkit` 是车载调试工具终端项目，面向设备配置、状态查看、网络诊断、实时数据展示等场景。所有新增代码都应优先遵循仓库现有工程体系，而不是引入新的架构范式。

## 基本原则

- 优先复用现有组件、hooks、store、services、工具函数和样式体系
- 不要随意新增依赖；只有在明确无法用现有能力实现时才考虑引入
- 不要破坏 monorepo 的包边界，公共能力优先沉淀到 `packages/*` 或现有共享模块
- 页面实现应服务于车载终端的稳定性、可读性和可维护性，而不是演示型写法
- 所有代码修改应尽量保持最小变更原则，避免连带影响其他业务域

## 技术栈约束

- 使用 `React 18 + TypeScript + Vite`
- UI 以 `Ant Design`、`MUI`、`Tailwind CSS`、`Less` 的现有组合为准，避免随意混搭新的 UI 范式
- 数据流优先遵循现有的 `zustand` / `ahooks` / `service` 组织方式
- 国际化默认通过 `i18next` 管理，禁止在可见文案中直接硬编码业务文本

## 业务目标

该项目属于车载调试终端，典型目标包括：

- 设备连接与状态监控
- 网络配置与诊断
- 车辆/终端信息展示
- 实时消息或状态刷新
- 大屏或终端场景下的高可读性展示

因此，所有新增功能都要优先考虑：

- 弱网或离线情况下的可用性
- 结果明确的空状态、错误态、加载态
- 业务术语的一致性
- 在终端设备上的性能表现

## 编码风格

- 组件、页面、服务、类型、常量要按职责拆分
- 不要把接口请求、数据转换和 UI 逻辑全部写在同一个组件里
- 命名必须语义化，避免 `data1`、`temp`、`listA` 这类含糊命名
- 新代码要尽量与仓库既有风格保持一致，不要追求与全局风格断裂的“个人审美”
