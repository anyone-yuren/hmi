---
description: 状态管理与数据流规范，约束本地状态、全局状态、派生状态与实时状态
alwaysApply: true
---

# 状态管理与数据流规范

## 状态分层

- 组件内部状态只保存当前页面或局部交互所需的信息
- 跨页面、跨组件共享的状态放入全局 store
- 来自接口、轮询、WebSocket 或 SignalR 的数据要单独归类管理
- 派生状态不要重复落地存储，能计算出来的就不要再存一份

## 单一数据来源

- 一个状态尽量只有一个权威来源
- 不要在多个地方同时维护同一业务字段的副本
- 接口返回数据应作为只读来源，修改前先明确是否需要做本地缓存或表单草稿

## 选中态、加载态、错误态

- 选中项、loading、error、empty 等 UI 状态要显式建模
- 不要把业务数据与界面状态混在同一个结构里，除非两者确实无法分离
- 切换页面或重新拉取数据时，要明确是否需要重置选中项

## 适用于 toolkit 的场景

- 网络选择、设备选择、节点选择等场景，要避免选中态与列表刷新相互污染
- 实时数据刷新时，要尽量保持用户当前查看内容不被意外打断
- 设备离线、连接中、连接失败、恢复中等状态要可区分，不能只用一个“失败”笼统表示
