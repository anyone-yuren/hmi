---
description: 接口请求与 services 规范，约束请求封装、类型和错误处理
alwaysApply: true
---

# 接口请求与 `services` 规范

## 请求封装

- 所有接口调用优先放在 `services` 层，不要直接散落在页面组件里
- 页面只负责调用 service 并处理展示结果
- 接口参数和返回值要尽可能类型化，减少对后端字段名的硬编码依赖

## 数据处理

- 进入 UI 层之前，应完成必要的数据归一化和字段转换
- 复杂字段映射、枚举翻译、状态文案拼装等逻辑不要散落在多个组件里
- 对接口响应中的可选字段要做健壮处理，不能默认一定存在

## 错误处理

- 所有请求都要考虑失败、超时和空数据的展示逻辑
- 不要只在控制台打印错误而不做页面反馈
- 对车载调试类接口，优先提供可操作的失败提示，如重试、刷新或恢复连接

## 接口组织

- 同一业务域的接口应集中管理，避免按页面零碎分散
- 接口命名要表达业务意图，不要只按照 HTTP 动词机械命名
- mock 和真实接口的入参与返回结构要尽量保持一致
