---
description: toolkit 车载调试业务域规范，约束术语、页面语义、状态表达和操作习惯
alwaysApply: true
---

# `toolkit` 车载调试业务域规范

## 领域语义

- 所有页面和功能实现都应围绕车载调试终端的实际工作流展开
- 业务命名应直接表达领域含义，避免用过度抽象的前端术语替代真实业务概念
- 车辆、网络、设备、诊断、告警、充电、终端状态等概念要保持一致表达

## 页面表达

- 页面布局优先服务于现场调试、排查和确认效率
- 关键状态应放在显眼位置，避免让用户在多个区域之间反复查找
- 操作按钮、状态标签、提示语应和业务动作一一对应，避免模糊表达

## 状态体系

- 状态值应尽量完整覆盖真实业务场景，例如：正常、异常、连接中、已连接、离线、未知、失败、重试中
- 不要把多个不同问题都塞进同一个“异常”状态里
- 如果某个状态对业务决策有影响，就必须在 UI 上明确体现

## 操作习惯

- 终端场景更重视效率和稳定性，交互应尽量直观、少步骤、少歧义
- 对于高频使用功能，要优先保证操作路径短、反馈快、结果明确
- 危险操作、不可逆操作、会影响设备状态的操作必须有明确确认机制

## 业务实现要求

- 新功能开发前要先明确其所属业务域和上下游依赖
- 与设备侧、服务侧、后端状态同步有关的页面要考虑同步延迟和状态不一致问题
- 如果存在本地模拟数据、调试数据或 mock 数据，其结构和命名要尽量贴近真实业务