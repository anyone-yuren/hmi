---
description: 性能规范，约束列表渲染、重渲染、懒加载和高频更新处理
alwaysApply: true
---

# 性能规范

## 列表与大数据量展示

- 长列表优先考虑虚拟滚动或分页
- 列表项渲染要保持轻量，避免在每一项中放入过重的计算逻辑
- 列表更新时应尽量保持稳定 key，减少不必要的重排和闪烁

## 重渲染控制

- 高频更新区域应尽量拆成更细粒度组件
- 只有在确有收益时才使用 `useMemo`、`useCallback`，避免为了“看起来优化”而过度包裹
- 不要把可计算值重复存成状态，避免额外渲染

## 资源与异步加载

- 大体积资源、复杂图表、3D 相关模块和低频页面应考虑懒加载
- 对非首屏功能应尽量按需引入，降低初始包体积
- 轮询、订阅、定时刷新要控制频率，避免对终端造成额外压力

## toolkit 场景

- 车载调试终端强调稳定和实时响应，性能问题往往会直接影响可用性
- 页面应尽量避免因为局部刷新导致整页卡顿或状态抖动
