# RobotToolkit 功能需求清单

本文档详细列出了 RobotToolkit 项目（Vehicle HMI）的核心功能模块及其具体需求。

## 1. 3D 场景与建模 (Models)

### 1.1 库位点偏移表 (OffsetTable)
基于 3D 场景的库位点可视化与偏移量管理组件。

*   **高性能渲染**
    *   支持 10,000+ 库位点的流畅渲染（使用 `InstancedMesh`）。
    *   支持视锥体剔除（Frustum Culling）和距离剔除（LOD），优化大规模数据下的性能。
*   **可视化展示**
    *   **库位点绘制**: 在 X-Y 平面上绘制库位点。
    *   **颜色标识**:
        *   默认状态: 灰色 (`#d9d9d9`)
        *   选中状态: 绿色 (`#00ff00`)
        *   有偏移值: 黄色 (`#faad14`)
    *   **智能标签**:
        *   仅在相机缩放（Zoom）< 30 时显示库位 ID。
        *   根据缩放级别动态调整标签显示数量，优先显示视野中心附近的标签。
        *   限制最大同屏标签数量（50个）以保证性能。
*   **交互操作**
    *   **单选**: 点击单个库位点进行选择/取消选择。
    *   **框选**: 鼠标拖拽绘制矩形框，批量选中区域内的库位点。
    *   **加选**: 按住 Ctrl/Cmd 键进行叠加选择或叠加框选。
    *   **导航**:
        *   右键拖拽: 平移/旋转场景 (MapControls)。
        *   双击: 重置相机视图。
        *   滚轮: 缩放场景。
*   **编辑功能**
    *   **偏移量修改**: 选中库位点后，通过悬浮面板 (`Rnd` 可拖拽组件) 修改 X/Y/Theta 偏移量。
    *   **批量修改**: 支持同时修改多个选中库位点的偏移量。
    *   **右键菜单**: 单个库位点支持右键选中并直接弹出修改面板。

### 1.2 自动化编译编排 (AutoCompile)
基于节点流的自动化构建与依赖管理组件。

*   **流程编排 (React Flow)**
    *   **分层设计**: 包含依赖库 (Dependency)、基础层 (Base)、应用层 (Application) 三个层级。
    *   **节点管理**:
        *   支持添加新节点（指定层级、名称、数据来源）。
        *   节点内支持勾选启用/禁用。
        *   节点内支持版本号下拉选择。
    *   **连线关系**: 预设层级间的依赖连线。
*   **依赖校验**
    *   **强制依赖**: 当勾选某个节点时，自动检查其依赖节点是否已勾选（如 `mwrobot_driver_laser` 依赖 `mwrobot_msgs`）。
    *   **编译限制**: 若依赖未满足，禁用“启动编译”按钮并显示具体的依赖缺失提示。
*   **编译执行**
    *   **进度反馈**: 启动编译后，底部面板滑出显示编译进度条动画。
    *   **状态模拟**: 模拟编译耗时与完成状态。
*   **历史记录 (History)**
    *   **侧边栏查看**: 点击顶部按钮，右侧滑出构建历史列表。
    *   **快照回溯**: 点击任意历史记录，主画布切换为只读模式，展示该次构建时的节点快照数据。
    *   **只读提示**: 预览历史快照时，顶部显示 Banner 提示，并提供退出预览按钮。

### 1.3 视觉流程配置 (VisionFlow)
用于配置机器视觉相关参数与流程的节点编辑器。

*   **节点类型**:
    *   VisionConfigNode: 视觉基础配置。
    *   VisionParamNode / VisionParamGroupNode: 参数设置。
    *   VisionSceneNode: 场景定义。
    *   VisionScenePickupMove / VisionSceneSelectNode: 具体的视觉任务节点（抓取、移动、选择）。
*   **流程连线**: 定义视觉处理的执行顺序与数据流向。

## 2. 2D 地图与任务管理 (SingleTask)

### 2.1 地图可视化 (InitStage)
*   **元素渲染**:
    *   绘制点 (Points)、线 (Lines/Paths)。
    *   绘制车辆 (AGV) 实时位置与状态。
    *   绘制多边形区域与背景地图。
*   **交互**: 缩放、平移、点击元素查看详情。

### 2.2 任务管理
*   **任务下发**:
    *   创建点到点任务。
    *   配置任务参数（如举升高度、速度阈值等）。
*   **任务面板**:
    *   查看当前任务队列。
    *   取消/暂停任务。
*   **RCS 任务集成**: 与 RCS (Robot Control System) 交互，下发与监控复杂调度任务。

### 2.3 偏移量管理 (2D)
*   提供 2D 视图下的库位偏移量查询与修改功能（作为 3D OffsetTable 的补充或替代）。

## 3. MES 工作流设计器 (WorkflowDesigner)

专为 MES (制造执行系统) 业务设计的工作流编辑器。

*   **节点编辑**
    *   **拖拽创建**: 从侧边栏拖拽 Start, End, Process, Control 等预置节点。
    *   **智能连接**: 拖拽连线或点击句柄自动创建并连接新节点。
    *   **属性面板**: 编辑选中节点的名称、描述及业务参数。
*   **辅助功能**
    *   **历史记录**: 支持撤销 (Undo) 和重做 (Redo)。
    *   **流程验证**: 保存时检查孤立节点与连通性。
    *   **暗黑模式**: 适配深色 UI 主题。

## 4. 系统与维护模块

### 4.1 充电管理 (Charging)
*   **电池监控**: 实时显示车辆电量、电压、电流。
*   **充电历史**: 记录充电开始/结束时间与电量变化。
*   **状态动画**: 充电中的可视化反馈。

### 4.2 维护工具 (Maintenance)
*   **硬件控制**: 举升装置控制、复位操作。
*   **传感器监测**: 查看各类传感器实时状态。
*   **运行状态**: 车辆运行模式切换与监控。

### 4.3 系统信息 (About)
*   **日志查看**: 节点运行日志 (Node Logs)。
*   **系统面板**: 核心服务状态监控。

## 5. 参数配置 (Parameters)

*   **基础信息**: 车辆基础参数配置。
*   **路径规划**: 导航与路径规划算法参数调整。
