import UserCard from '../components/userCard';

import type { FC } from 'react';

interface PProFile {}

const Device: FC<PProFile> = () => {
  return <UserCard></UserCard>;
};

export default Device;
